<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Dùng cache-path nếu lưu vào getTemporaryDirectory() hoặc getApplicationCacheDirectory() -->
    <cache-path name="cached_files" path="." />
    <!-- Hoặc external-cache-path nếu lưu vào getExternalCacheDirectories() -->
    <!-- <external-cache-path name="external_cached_files" path="." /> -->
    <!-- Hoặc external-path nếu lưu vào getExternalStorageDirectory() (ít dùng hơn) -->
    <!-- <external-path name="external_files" path="." /> -->
</paths>